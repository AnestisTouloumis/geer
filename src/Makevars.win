## C++17 is required: std::string_view is used in family_utils.h and link_utils.h.
## R >= 4.0 on all CRAN platforms supports CXX17 via RcppArmadillo.
CXX_STD = CXX17

PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
